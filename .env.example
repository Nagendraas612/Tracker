# Database (Optional - defaults to automatic persistent local storage if MongoDB is not provided)
MONGODB_URI=mongodb://127.0.0.1:27017/sih_tracker

# ntfy Push Notification Gateway
NTFY_SERVER_URL=https://ntfy.sh

# Continuous Monitoring Worker Interval (in milliseconds)
POLL_INTERVAL_MS=30000

# Live SIH 2026 Production Mode (false = live real-time scraping)
DEMO_MODE=false
